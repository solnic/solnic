---
title: GitHub Sponsors
author: Peter Solnica
date: <%= Time.now.strftime('%Y-%m-%dT%H:%M:%S%:z') %>
---

<h2>Current Sponsors</h2>

<p>I'm incredibly grateful for the support from my current GitHub Sponsors. Your contributions help me dedicate more time to open source work and make it sustainable. Thank you! 🙏</p>

<div class="sponsors-grid current-sponsors">
<% current_sponsors.each do |sponsor| %>
  <div class="sponsor-card">
    <a href="<%= github_profile_url(sponsor['sponsor_handle']) %>" target="_blank" rel="noopener noreferrer" class="sponsor-link">
      <% if sponsor['avatar_url'] %>
        <img src="<%= sponsor['avatar_url'] %>" alt="<%= sponsor['sponsor_name'] %>" class="sponsor-avatar">
      <% end %>
      <div class="sponsor-info">
        <h3><%= sponsor['sponsor_name'] %></h3>
      </div>
    </a>
  </div>
<% end %>
</div>

<h2>Past Sponsors</h2>

<p>Thank you to all my past sponsors for their support! 🙏</p>

<div class="sponsors-grid past-sponsors">
<% past_sponsors.each do |sponsor| %>
  <div class="sponsor-card">
    <a href="<%= github_profile_url(sponsor['sponsor_handle']) %>" target="_blank" rel="noopener noreferrer" class="sponsor-link">
      <% if sponsor['avatar_url'] %>
        <img src="<%= sponsor['avatar_url'] %>" alt="<%= sponsor['sponsor_name'] %>" class="sponsor-avatar">
      <% end %>
      <div class="sponsor-info">
        <h3><%= sponsor['sponsor_name'] %></h3>
      </div>
    </a>
  </div>
<% end %>
</div>
